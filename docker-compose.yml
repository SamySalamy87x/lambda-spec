services:
  lambda-spec:
    build: .
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.6}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    restart: unless-stopped
